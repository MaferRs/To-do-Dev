import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginBottom: 16,
  },
  label: {
    fontSize: 16,
    marginBottom: 8,
  },
  input: {
    width: 350,
    height: 50,
    backgroundColor: '#EDEDED',
    borderRadius: 20,
    paddingLeft: 20,
    fontSize: 16,
  },
});
